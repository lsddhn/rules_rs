bazel_dep(name = "libssh2", version = "1.11.1.bcr.1")

crate = use_extension("@rules_rs//rs:extensions.bzl", "crate")
crate.annotation(
    crate = "libssh2-sys",
    gen_build_script = "off",
    deps = ["@libssh2"],
)

inject_repo(crate, "libssh2")
